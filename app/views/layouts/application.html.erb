<!DOCTYPE html>
<html>
  <head>
    <title>Boacausa2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="bg-gray-800">
      <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
        <div class="relative flex items-center justify-between h-16">
          <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
            <div class="flex space-x-4">
              <a href="<%= organizations_path %>" class="<%=nav_menu_item_class(organizations_path)%>">Organizations</a>
              <a href="<%= pets_path %>" class="<%=nav_menu_item_class(pets_path)%>">Pets</a>
            </div>
          </div>
        </div>
      </div>
    </nav>
    <main class="container mx-auto mt-24 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
